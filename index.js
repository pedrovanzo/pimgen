/**
 * 
 * @param {*} width px
 * @param {*} height px
 * @param {*} text string (optional)
 * @param {*} color hexadecimal (optional)
 * @returns 
 */
function pimgenGenerate(width, height, text, color) {
    // img generation logic
    return // image with width and height
}